using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GolePointObj : MonoBehaviour
{
    private Collider2D goleCollider;

    // Start is called before the first frame update
    void Start()
    {
        goleCollider.GetComponent<Collider2D>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
